# Dev / CI / migration / optional-ML dependencies.
# Installs the runtime set plus everything needed for tests, migrations,
# local serving, and real ONNX embeddings.
-r requirements.txt
uvicorn[standard]==0.34.0
pytest==8.3.4
pytest-asyncio==0.25.0
alembic==1.14.0
pgvector==0.3.6
psycopg2-binary==2.9.10
# Optional: real ONNX all-MiniLM-L6-v2 embeddings. Without this the app
# transparently uses the deterministic hashing matcher (see embeddings/local.py).
fastembed==0.5.1
